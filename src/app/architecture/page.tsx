import Link from "next/link";
export default function Architecture() { return <div className="page prose-page"><div className="eyebrow">SYSTEM DESIGN</div><h1>One financial foundation</h1><p className="lead">Order writes pass through an authenticated Next.js route and a PostgreSQL transaction. The database owns price lookup, discount approval, rate validation, and saved totals.</p><div className="architecture-flow">{["Browser / Next.js App Router", "Authenticated route handler", "Supabase Auth JWT", "PostgreSQL RPC + RLS", "Immutable orders and audit events"].map((item, i) => <div key={item}><div className="flow-box"><span>0{i + 1}</span><strong>{item}</strong></div>{i < 4 && <div className="flow-arrow">↓</div>}</div>)}</div><div className="architecture-columns"><section><h2>Tenant boundaries</h2><p>Every business table carries <code>organization_id</code>. RLS checks active membership. Financial tables have no direct adviser write path.</p></section><section><h2>Historical money</h2><p>Orders store USD cents, whole SDG, and the integer SDG-per-USD rate captured at save time. Each order line stores its authoritative unit price. Historical rows reject updates and deletes.</p></section><section><h2>Owner approval</h2><p>An authenticated owner creates a database approval for one draft and line. The save transaction matches product, quantity, current database price, and discount before accepting a restricted discount.</p></section><section><h2>Future modules</h2><p>Receipts and currency conversions should store their own currency and rate snapshots. Cost prices belong in an owner-only table or private schema with separate grants, never in the adviser-readable product catalogue.</p></section></div><Link href="/tests" className="button secondary">See verification →</Link></div>; }
